name: flutter_application_1
description: A new Flutter project

environment:
  sdk: ">=3.0.0 <4.0.0" # Assurez-vous d'adapter la version SDK à votre projet

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+2 # Vérifiez la compatibilité avec votre projet

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0 # Bonne pratique pour le linting

flutter:
  uses-material-design: true
